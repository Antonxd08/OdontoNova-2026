<Window x:Class="OdontoNova_2026.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="White">

    <!--Efecto sobre las tarjetas de push -->
    <Window.Resources>
        <Style x:Key="CardHoverStyle" TargetType="Border">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                             To="1.03" Duration="0:0:0.15"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                             To="1.03" Duration="0:0:0.15"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                             To="1" Duration="0:0:0.15"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                             To="1" Duration="0:0:0.15"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Contenedor de toda la pantalla -->
        <Border Background="White" BorderBrush="#E5E7EB" BorderThickness="1">
            <Grid>
                <!-- ============ ENCABEZADO ============ -->
                <Border Background="#2F80ED" Height="40"
                    VerticalAlignment="Top" MouseDown="Header_MouseDown">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!--  ============ VENTANA BOTONS ============  -->
                        <StackPanel Orientation="Horizontal" Grid.Column="2" VerticalAlignment="Center" Margin="0,0,8,0">
                            <Button Content="—" Width="36" Height="30"
                                Background="Transparent" Foreground="White"
                                BorderThickness="0" FontSize="14"
                                Cursor="Hand" Click="Min_Click"/>
                            <Button Content="▭" Width="36" Height="30"
                                Background="Transparent" Foreground="White"
                                BorderThickness="0" FontSize="14"
                                Cursor="Hand" Click="Max_Click"/>
                            <Button Content="✕" Width="36" Height="30"
                                Background="Transparent" Foreground="White"
                                BorderThickness="0" FontSize="14"
                                Cursor="Hand" Click="Close_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- ============ CONTENIDO PANTALLA ============ -->
                <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled" Margin="0,50,0,20">
                    <StackPanel HorizontalAlignment="Center" Width="600" Margin="0,0,0,14">

                        <!-- ====== LOGO PRINCIPAL ====== -->
                        <Image x:Name="LogoImage"
                           Source="/assets/Images/LogoBlanco.png"
                           Stretch="Fill"
                           Width="450"
                           Margin="0,10,0,30"
                           Opacity="0.95"
                           HorizontalAlignment="Center"/>

                        <!-- ====== TARJETAS ====== -->
                        <ItemsControl x:Name="UserCards">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="1"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource CardHoverStyle}" Background="White" Padding="20" Margin="0,0,0,20" BorderBrush="#E2E8F0" BorderThickness="1" Cursor="">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#1A000000" BlurRadius="10" ShadowDepth="2" />
                                        </Border.Effect>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <!-- Iconos ciruclares -->
                                            <Border Width="54" Height="54" Background="{Binding ColorIcono}"  VerticalAlignment="Center" CornerRadius="27" Margin="0,0,16,0">
                                                <Image Source="{Binding RutaIcono}" Width="28" Height="28" Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </Border>

                                            <!-- Textos -->
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Titulo}" FontFamily="Tahoma" FontSize="18" FontWeight="Bold"  Foreground="#1E293B"/>
                                                <TextBlock Text="{Binding Subtitulo}" FontSize="12" Foreground="#64748B" Margin="0,2,0,0"/>
                                            </StackPanel>

                                            <Button Grid.Column="2" Content="Ingresar" Width="110" Height="38" Padding="8,0" Margin="20,0,0,0" Background="#2F80ED" Foreground="White" FontWeight="SemiBold" BorderBrush="Transparent"  Cursor="Hand" VerticalAlignment="Center" Click="BtnIngresar_Click">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Background" Value="#2F80ED"/>
                                                        <Setter Property="Foreground" Value="White"/>
                                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="Button">
                                                                    <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#1E6FDB"/>
                                                                <Setter Property="Effect">
                                                                    <Setter.Value>
                                                                        <DropShadowEffect Color="#33000000" BlurRadius="8" ShadowDepth="1"/>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Trigger>
                                                            <Trigger Property="IsPressed" Value="True">
                                                                <Setter Property="Background" Value="#175BB5"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </Grid>
                                    </Border>

                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>

</Window>
